services:
  metabase:
    image: metabase/metabase:latest
    container_name: metabase
    ports:
      - "3000:3000"
    environment:
      - MB_DB_FILE=/data/metabase.db
    volumes:
      - "./:/data:Z"
    user: "0:0" 
    restart: always